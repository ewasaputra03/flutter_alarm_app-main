name: alarm_app
description: A new alarm_app
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  charts_flutter: ^0.11.0
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.1
  flutter_local_notifications: ^9.1.5
  intl: ^0.17.0
  rxdart: ^0.27.3
  vector_math: ^2.1.0

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:


  uses-material-design: true

